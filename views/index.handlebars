{{!-- 
 <div class="row">
    {{#each article}}
        <h4>{{this.title}}</h4>
        {{this.text}}
           <form method="POST" action="/articles/save/{{this._id}}?_method=PUT">
           <button type="submit" class="button btn btn-warning">Choose</button>
           </form>
    {{/each}}
  </div> --}}
 {{#each article}}
<div class="card">
  <div class="card-header">
  
  </div>
  <div class="card-body">
    <h5 class="card-title">{{this.title}}</h5>
    <div class="btn-group">
    <a href="{{this.link}}" class="btn btn-primary">View Article</a>

      <form method="POST" action="/articles/save/{{this._id}}?_method=PUT">
           <button type="submit" class="button btn btn-warning">Save Article</button>
           </form>
    </div>
  </div>

</div>
  </br>
    {{/each}}